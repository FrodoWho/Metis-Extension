{
  "name": "measure-extension",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "build:chrome": "node build.js --chrome",
    "build:firefox": "node build.js --firefox",
    "test": "node build.js --chrome && playwright test",
    "test:headed": "node build.js --chrome && playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
